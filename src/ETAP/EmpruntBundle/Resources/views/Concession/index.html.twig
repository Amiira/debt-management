{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Concession list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Datecreation</th>
                <th>Description</th>
                <th>Nom</th>
                <th>Valeurrealisation</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('concession_show', { 'id': entity.id }) }}">{{ entity.datecreation }}</a></td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.valeurrealisation }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('concession_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('concession_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('concession_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
