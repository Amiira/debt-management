{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Demande list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Datedemande</th>
                <th>Montantemprunt</th>
                <th>Nom</th>
                <th>Refdemande</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('demande_show', { 'id': entity.id }) }}">{{ entity.datedemande }}</a></td>
                <td>{{ entity.montantemprunt }}</td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.refdemande }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('demande_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('demande_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('demande_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
