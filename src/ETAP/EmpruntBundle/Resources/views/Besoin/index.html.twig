{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Besoin list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Datebesoin</th>
                <th>Description</th>
                <th>Nom</th>
                <th>Valeur</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('besoin_show', { 'id': entity.id }) }}">{{ entity.datebesoin }}</a></td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.valeur }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('besoin_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('besoin_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('besoin_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
